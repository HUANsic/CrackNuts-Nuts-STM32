# 📘 Nut 通信协议与指令集 API 文档 (2024/04/21)

- **项目名称**: Cracknuts-Nuts-STM32
- **适用分支**: FullFunction (集成所有功能)
- **底层驱动**: NutsLib.c
- **字节序**: 大端序 (Big-Endian)

---

## 1. 通信帧结构

系统采用定长头部 + 变长负载的帧结构，请求与响应的头部定义略有不同。

### **1.1 请求帧 (Request)**

由上位机发送至 STM32。

| 字段          | 长度 (Bytes) | 描述                           |
|:----------- |:---------- |:---------------------------- |
| **Command** | 2          | **命令码**，指定操作类型 (如 `0x0001`)。 |
| **RFU**     | 2          | **保留字段**，通常填充 `0x0000`。      |
| **Length**  | 4          | **数据长度**，表示 Payload 的字节数。    |
| **Payload** | N          | **数据负载**，具体格式见指令说明。          |

### **1.2 响应帧 (Response)**

由 STM32 返回至上位机。

| 字段          | 长度 (Bytes) | 描述                               |
|:----------- |:---------- |:-------------------------------- |
| **Status**  | 2          | **状态码**，指示执行结果 (如 `0x0000` 为成功)。 |
| **Length**  | 4          | **数据长度**，表示 Payload 的字节数。        |
| **Payload** | N          | **数据负载**，包含返回的数据或错误信息。           |

> **注意**: 所有的多字节数值（Command, Length, Status 以及 Payload 中的整数）均采用 **大端序 (Big-Endian)** 传输。

---

## 2. 状态码定义

| 状态码 (Hex) | 宏定义                      | 描述                  |
|:--------- |:------------------------ |:------------------- |
| `0x0000`  | `NUT_OK`                 | 执行成功。               |
| `0xFFFF`  | `NUT_ERROR`              | 通用错误。               |
| `0xFFFE`  | `NUT_ERROR_USER_CODE`    | 用户代码逻辑错误 (如参数校验失败)。 |
| `0xFFFD`  | `NUT_ERROR_PAYLOAD_SIZE` | 缓冲区溢出或数据长度不匹配。      |

---

## 3. 指令集详解

系统共支持 **16** 条指令，分为五大类。

### 3.1 基础与趣味指令 (`0x00xx`)

| 命令码      | 名称             | 功能描述  | 输入 Payload   | 输出 Payload                               |
|:-------- |:-------------- |:----- |:------------ |:---------------------------------------- |
| `0x0001` | **Echo**       | 回显测试  | 任意数据         | 原样返回输入数据                                 |
| `0x0002` | **ReadSecret** | 读取冷知识 | 任意 (可选作随机种子) | ASCII 字符串 (如 "Sea otters hold hands...") |

### 3.2 AES 软件加密指令 (`0x01xx`)

*算法: AES-128-ECB*

| 命令码      | 名称                | 功能描述   | 输入 Payload | 输出 Payload |
|:-------- |:----------------- |:------ |:---------- |:---------- |
| `0x0100` | **AES_SetEncKey** | 设置加密密钥 | 16 字节密钥    | 无          |
| `0x0101` | **AES_SetDecKey** | 设置解密密钥 | 16 字节密钥    | 无          |
| `0x0102` | **AES_Encrypt**   | 软件加密   | 16 字节明文    | 16 字节密文    |
| `0x0103` | **AES_Decrypt**   | 软件解密   | 16 字节密文    | 16 字节明文    |

### 3.3 AES 硬件加速指令 (`0x02xx`)

*利用 STM32 CRYP 外设加速*

| 命令码      | 名称                 | 功能描述 | 输入 Payload | 输出 Payload |
|:-------- |:------------------ |:---- |:---------- |:---------- |
| `0x0200` | **AES_HW_Encrypt** | 硬件加密 | 16 字节明文    | 16 字节密文    |
| `0x0201` | **AES_HW_Decrypt** | 硬件解密 | 16 字节密文    | 16 字节明文    |

### 3.4 RSA 大数运算指令 (`0x04xx`)

*支持可变长度密钥 (如 1024/2048 位)，需按 4 字节对齐传输*

| 命令码      | 名称                | 功能描述 | 关键输入参数                                             |
|:-------- |:----------------- |:---- |:-------------------------------------------------- |
| `0x0400` | **RSA_SetPubKey** | 设置公钥 | 4B长度 + 4B指数e + N字节模数n                              |
| `0x0401` | **RSA_SetPriKey** | 设置私钥 | 4B长度 + 4B指数e + N字节模数n + d + p + q + dP + dQ + qInv |
| `0x0402` | **RSA_Encrypt**   | 公钥运算 | 4B长度 + N字节数据 (用于加密/验签)                             |
| `0x0403` | **RSA_Decrypt**   | 私钥运算 | 4B长度 + N字节数据 (用于解密/签名)                             |

> **注意**: RSA 指令的 Payload 中，长度字段也是 4 字节大端序。

### 3.5 RSA 32位简化指令 (`0x7Fxx`)

*专为教学演示设计，仅处理 32 位整数*

| 命令码      | 名称                | 功能描述  | 输入 Payload                 | 输出 Payload |
|:-------- |:----------------- |:----- |:-------------------------- |:---------- |
| `0x7F00` | **RSA32_SetPub**  | 设置公钥  | 无 (使用硬编码默认值)               | 无          |
| `0x7F01` | **RSA32_SetPri**  | 设置私钥  | 无 (使用硬编码默认值)               | 无          |
| `0x7F02` | **RSA32_Encrypt** | 32位加密 | 4 字节整数 (位于 Payload 偏移 4 处) | 4 字节结果     |
| `0x7F03` | **RSA32_Decrypt** | 32位解密 | 4 字节整数 (位于 Payload 偏移 4 处) | 4 字节结果     |

---

# 4. 调试与状态指示

在部分耗时指令执行期间，STM32 会提供物理状态指示：

- **LED**: 在执行 `RSA` 或 `AES` 运算时，板载 LED 会点亮，运算结束熄灭。
- **IO_1 / Trigger**: 在执行 `RSA32` 或 `AES` 运算时，特定 GPIO 引脚会拉高，可用于示波器或逻辑分析仪触发抓包。


